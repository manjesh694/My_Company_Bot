version: '3.8'

services:
  my_company_bot:
    build: .
    container_name: my_company_bot_container
    ports:
      - "8000:8000"
      - "7860:7860"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always